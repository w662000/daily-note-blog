---
layout: default
title: 交接文档 · 进度_20260817_1241 · 老技术点脚本暂停处置（不干扰新脚本自动发）
date: 2026-08-17 23:30:00 +0800
---

# 进度_20260817_1241 · 老技术点脚本暂停处置（不干扰新脚本自动发）

- **日期**：2026-08-17
- **状态**：✅ 已完结（scan 自动收集）
- **来源**：2026-08-17-00-05-49\进度_20260817_1241_老脚本暂停不干扰.md

> 用户指令（12:41）：「老脚本暂停就行 不要干扰新脚本自动发」

## 结论
老技术点机制已全部**隔离暂停（不删除、保留可回滚）**，且已用数据库只读核查**实锤**：14 个自动化里没有任何任务实际调用老脚本。新链 `techpoint_flow.py` 的自动发布不会被干扰。

## 处置动作
1. **沿用上轮隔离的 4 个老脚本**（保持暂停、不删）：
   - `publish_techpoints.py` / `forum_publish_techpoint.py` / `yuque_publish_techpoint.py` / `build_techpoint_list.py`
   - 位置：`D:\AI work\workbuddy\handoff\_deprecated_old_techpoint\`
2. **新发现第 5 个同机制脚本 `gen_autoclaw_docs.py`**（Grep 活跃代码抓出）：
   - 它会直接往博客源 `_handoffs/` 写**老格式**技术点 `2026-08-DD-techpoint-autoclaw-*.md`（带日期前缀），注释写明"走 `publish_techpoints.py`"；硬编码 `TODAY_YYMMDD=260812` 的一次性脚本。
   - 一并移入 `_deprecated_old_techpoint/` 隔离暂停，杜绝"老格式技术点复活干扰新链"。
3. **数据库只读核查**（用 python 只读 `workbuddy.db`，未删未改）：
   - 命中 `publish_techpoints` 的只有 `1784951947030`(23:07) 与 `automation-1785646936067`(11:00)；
   - 23:07 主链 prompt 中该名仅为"如需齐平 4 端可恢复调用"的**注释性说明**（默认不 subprocess 老脚本）；
   - 11:00 巡检 prompt 中三老脚本名是"**严禁调用**"禁令文字；
   - `gen_autoclaw_docs` 不在任何自动化任务 prompt 中 → 无自动调用。

## 当前状态
- 活跃 `handoff/` 目录仅剩新链 `techpoint_flow.py`（已复核：`ls` 返回仅此一个相关脚本）。
- 技术点**唯一生产/发布链路** = 23:07 主链 `techpoint_flow.py` scan→publish（博客源 + 有道云第5端），与两条铁保证一致。
- 老脚本处于"暂停"态：文件仍在 `_deprecated_old_techpoint/`，可回滚，但脱离所有自动调用路径。

## 为何不会干扰新脚本自动发（证据链）
| 检查点 | 结果 |
|---|---|
| 活跃目录有无老脚本 | 无（仅 `techpoint_flow.py`） |
| 自动化是否实际 subprocess 老脚本 | 无（仅注释/禁令文字） |
| 老脚本能否捞到新链文件 | 不能（命名不兼容：老认 `YYYY-MM-DD-techpoint-`，新产 `techpoint-`） |
| 老格式技术点是否还会被写入 `_handoffs` | 不会（`gen_autoclaw_docs.py` 已隔离，且仅手动运行） |
| `_handoffs` 历史重复 | 已清（10 篇移 `_legacy_techpoint_dups`，40 篇冻结 bak） |

## 遗留
- 有道云第5端 `upload_youdao.py` 上次退出码 1 仍未深挖（单点问题，不影响博客源+Gridea 两端核心修复）。
- `_deprecated_old_techpoint/` 5 个脚本为**暂停隔离、未硬删**，用户明确"不删"；如需彻底删除另行指示。

## 记忆落盘
- 项目 MEMORY.md：新增「老技术点脚本处置」段（含用户红线：暂停不删、不得干扰新链）。
- 今日日志 `2026-08-17.md`：追加 12:41 处置记录。
