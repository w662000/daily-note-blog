---
layout: default
title: 交接文档 · 微信小程序开发大赛 API 接入 WB — 诊断(2026-08-18 23_04)
date: 2026-08-20 23:30:00 +0800
---

# 微信小程序开发大赛 API 接入 WB — 诊断(2026-08-18 23:04)

- **日期**：2026-08-18
- **状态**：✅ 已完结（scan 自动收集）
- **来源**：2026-08-18-12-42-34\进度_20260818_2304_微信大赛API.md

## 结论(修正版 23:16)
1. **截图后台写明的真实模型名是 `Deepseek-v4-flash`**(注意:首字母 D 大写,s 小写,不是 `DeepSeek-v4-flash`,也不是 `deepseek-v4-flash`)。该模型**已调通**,返回 200,真实映射模型为 `cp_dsv4_flash_0731_pro5000`。
2. **`GLM-5.2` 也存在,但当前 token 无权限**:实测返回 `user has no access`(429)。不是名字错,是 token 没被授权(可能还没开通,或本次比赛 token 只给 Deepseek-v4-flash)。
3. 之前我按搜索结果认定模型名是 `GLM-5.2`,这是**错误结论**。应以你截图后台显示为准。

## 实测证据(23:16 重测)
- `GET /openai/v1/models` → `missing required parameter: model`(该网关不实现标准 /models 列举,必须知道确切 model id)。
- `Deepseek-v4-flash`(后台截图精确拼写) → **HTTP 200**,正常返回 completion,响应中 `model` 字段为 `cp_dsv4_flash_0731_pro5000`。
- `deepseek-v4-flash`(小写 d) / `DeepSeek-v4-flash`(大写 S) → `model name not found`。
- `GLM-5.2` → `user has no access`(429)。
- 这说明该 API **严格大小写敏感**,且后台显示的拼写就是真实 model id。

## WB models.json 关键结构(实测)
- 字段只有:`id, name, vendor, url, apiKey, supportsToolCall, supportsImages, supportsReasoning, useCustomProtocol, caps`。**无独立 `model` 覆盖字段** → `id` 即发给 API 的 model 名。
- 现有 `id='GLM-5.2'` 条目:`vendor=SenseNova, url=https://token.sensenova.cn/v1`。

## 待用户决策(WB 里 GLM-5.2 槽位归属)
- A: 把现有 GLM-5.2 槽位改成 WeChat(改 url+apiKey,id 保持 GLM-5.2,显示名改 [WeChat] GLM-5.2)—— 会丢失 SenseNova 的 GLM-5.2。
- B: 保留 SenseNova,不往 WB 加 WeChat(已有 GLM-5.2 可用,但走的是 SenseNova 不是比赛 token)。
- C: 用户先去 chatapi.weixin.qq.com 后台确认 token 已激活、GLM-5.2 已开通,再决定。

## 最终执行(23:16 修正)
- 最初我误写成 `GLM-5.2`,重测后**按后台截图修正为 `Deepseek-v4-flash`**。
- 直接修改 WB `models.json` 中已写入的 WeChat 条目:把 `id` 从 `GLM-5.2` 改成 `Deepseek-v4-flash`,`name` 改成 `[WeChat] Deepseek-v4-flash (小程序大赛)`。
- `url` 保持 `https://chatapi.weixin.qq.com/openai/v1`(无尾斜杠,对齐 OpenAI 兼容约定)。
- 能力字段按后台截图设置:`supportsToolCall=true`(工具调用✓)、`supportsImages=false`(图片输入✗)、`supportsReasoning=true`(思考模式✓)。
- 校验:JSON 合法,共 33 个模型;`Deepseek-v4-flash`(WeChat,大写 D) 与 `deepseek-v4-flash`(b.ai,小写 d)**不重复 id**。
- **备份**:`models.json.bak-add-wechat-glm52-20260818-2310`(该备份仍是加 GLM-5.2 时的备份,后续 23:16 修正确保无问题)。
- **遗留**:若后台之后开通 `GLM-5.2`,可再追加一条 `id='GLM-5.2'`(大写,与 SenseNova 的 `glm-5.2` 不撞车);目前该 token 调 `GLM-5.2` 仍返回 429 `user has no access`。

## 接口/key(已知)
- 端点:`https://chatapi.weixin.qq.com/openai/v1/chat/completions`(OpenAI 兼容)
- key:`Vz9uhggBEAEaIAgBEhwxNzg3MDY1Mjk1MTk4MzAwNDMzOE9BdlNkV2Y5IhgIAxIUCAMSEA+H/iNO3ic9JBkQyWuufRg=`(敏感,仅本次会话使用,勿外泄)
- 限额(官方):每5小时≈1200次 / 每周≈9000 / 每月≈18000;不支持图片输入;工具调用+思考模式已开。
