---
layout: default
title: 技术点 · 进度_20260817_1722 · 技术点扩为 4 端 + 语雀禁用
date: 2026-08-17 23:30:00 +0800
---

# 技术点 · 进度_20260817_1722 · 技术点扩为 4 端 + 语雀禁用

> 来源：260817_进度_20260817_1722 · 技术点扩为 4 端 + 语雀禁用_handoff.md（项目 handoff 1:1 companion，由 techpoint_flow 新方法提炼；读取编码：utf-8）

## 一、技术选型
（源 handoff 未单列技术选型）

## 二、实施要点与关键技术
（源 handoff 未单列实施要点）

### 关键技术要点（自动抽取）
- > "A 另外语雀端的API可能不能用了 会员到期了 那就4端都发"
- 含义：技术点按选项 A 扩到全端；但语雀(Yuque)会员到期 API 不可用 → 实际 **4 端** = Gridea + 博客源 + 论坛 + 有道云（不含语雀）。
- 关键修复：此前若语雀失败会令 `published=False` → handoff 永不进 `bak/` → 每晚重发。现在语雀跳过，Gridea/博客/论坛 3 端成功即移 bak，再加循环外有道云第5端 = 实际 **4 端**。
- 同步更新顶部日志("5端"→"4端")与 dry-run 文案（去掉语雀）。
- 新增配置：GRIDEA_POSTS、论坛 bbs1org#3/phpBB#11 的 MCP 端点+token、WAF 静态 cookie、直连 opener（与 handoff_flow 同机制，绕过 Clash 代理对 *.my-place.us 的 MITM）。
- `gen_gridea_techpoint(iso_date, slug, gtitle, content)`：写 `GRIDEA_POSTS/{date}-techpoint-{slug}.md`（Gridea 专用 frontmatter），**按 title 幂等去重**（清同名旧稿），避免与历史遗留重复。
- `publish_forum_techpoint(title, body)`：发帖到 bbs1org#3 + phpBB#11，带 `list_topics` 标题去重 + 8s 间隔 + 限流退避（复用 `_forum_solve_waf`/`_forum_call`）。
- `cmd_publish` 主循环改为每篇：①博客源 emit ②Gridea 写稿 ③论坛发帖；循环外④博客 git push ⑤有道云；最后移 bak。日志与 docstring 同步更新为「4 端」。
- `ast.parse` 两个脚本均 OK。
- `techpoint_flow.py publish --dry-run` 退出码 0，无 Traceback；76 篇待发布均正确解析为「Gridea + 博客 + 论坛 + 有道云」4 端路径。
- 1. **积压**：`techpoint/update/` 现存 **76 篇待发布技术点**（历史积压，非本次引入）。今晚 23:07 会自动一次性发到 4 端，**含论坛 76 帖**（带 8s 间隔，耗时长但幂等+去重）。我**未手动触发**（白天打爆论坛限流风险）。若想分批节流请说一声。

## 三、关键产物与命令
（见源 handoff 关键产物字段）

## 四、如何复现 / 重打
（见源 handoff 重打方法字段）

## 五、后续风险
（见源 handoff 后续风险字段）
