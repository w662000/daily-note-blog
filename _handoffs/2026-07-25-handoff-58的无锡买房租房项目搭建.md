---
layout: default
title: 交接文档 · 58的无锡买房租房项目搭建
date: 2026-08-16 23:30:00 +0800
---

# 无锡买房 / 租房 · 58同城爬虫 — 交接文档（人读）

- **日期**：2026-07-25
- **状态**：✅ 已完结（新方法重生成）
- **来源**：handoff\bak\260725_58的无锡买房租房项目搭建_handoff.md（编码探测：utf-8）

> 更新于 2026-07-25 11:10（**买房 7/7 + 租房 7/7 全部完成，项目整体交付**）。项目根目录：`D:\AI work\workbuddy\2026-07-24-11-46-09\`
> 给接手的同学看。AI/agent 接手请直接读同目录的 `HANDOFF_AGENT.md`（更详细、含可执行命令）。
> 本文已同步发布到博客 `daily-note-blog`（Jekyll/GitHub Pages）与 Gridea Pro 待发布队列，见第 9 节。

---

## 0. 项目成功交付总结（一句话看成果）

**无锡 58 同城「二手房 + 租房」两套数据系统已全部上线，7/7 区全齐，看板可看全城数据：**

| 站点 | 状态 | D1 已落数据 | 说明 |
|---|---|---|---|
| **买房（wx-houseing）** | ✅ 已完成 | **7/7 区，3685 条**（梁溪1101/惠山760/新吴381/江阴369/宜兴368/滨湖362/锡山344） | 看板已可用，无需再动 |
| **租房（wxzf）** | ✅ 已完成 | **7/7 区，7466 条**（宜兴1154/新吴1143/锡山1140/江阴1121/滨湖1119/惠山1116/梁溪673） | 全部区已落 D1，看板已可用 |

**最大突破**：58 对沙箱出口 IP 在租房频道做了主动验证码墙（code=300），一度以为"浏览器验证救不了沙箱"。实测发现**用户在浏览器里手动过一次验证码，能给沙箱 IP 开一个宽松窗口**——2026-07-24 晚用户手动验证后连跑滨湖+惠山 2 个整区；2026-07-25 上午一次手动验证竟连跑锡山+新吴+江阴+宜兴 **4 个整区（各 50 页、0 撞墙）**，远超"1–2 区"的预期。

---

## 1. 我们在做什么

做一个「爬 58 同城 → 存云端 → 看板展示」的无锡房产数据系统，分两个独立频道：

- **买房（wx-houseing）**：抓 58「二手房（ershoufang）」频道。
- **租房（wxzf）**：抓 58「租房（zufang）」频道。

整体架构（**务必保留，别改**）：
```
本地 Playwright 无头浏览器爬虫  ──写入──>  local_crawl.db (listings 表)
        │
        │  db_to_sql.py 读 listings → 生成 import_data.sql
        ▼
wrangler d1 execute --remote  ──直连──>  Cloudflare D1  (wx-houseing-db / wxzf-db)
        │
        ▼
Worker (读接口) + Pages 看板 (用户浏览器访问)
```
> 为什么绕这么远：**国内 `*.workers.dev` 的 POST 被网络拦截**，所以爬虫不能直接推 Worker，必须落盘后 `wrangler` 直连 D1。

---

## 2. 已经完成的事（时间线）

1. **CF 全套资源已建好并部署**：2 个 D1（`wx-houseing-db` / `wxzf-db`）、2 个 KV、2 个 Worker、2 个 Pages 看板。
2. **买房站 100% 完成**：7 个区全部爬完并灌入 D1（3685 条），用户已能看到全城二手房数据。**不要再动它。**
3. **租房站梁溪区**：本地 782 → 过滤有效 673 已灌 D1（覆盖旧 797）。
4. **租房站滨湖区（7-24 晚）**：用户手动过验证码后，本地爬满 50 页、1291 条，过滤 **1119 条有效已灌入 D1 滨湖**（district_id=2），0 撞墙、完整。
5. **租房站惠山区（7-24 晚）**：紧接滨湖在同一窗口内跑通，本地 50 页、1279 条，过滤 **1116 条有效已灌 D1 惠山**（district_id=3），0 撞墙。（一度误判"部分中断"，实为后台仍在跑、stdout 缓冲未 flush；最终确认整区完成。）
6. **两个致命 bug 已修**（已写进 crawler.py，接手不用再修）：
   - Playwright `asyncio` 冲突：进程内只 `sync_playwright().start()` 一次，跨区只重开**浏览器**（`pw_close_browser`），不再重开整个实例。
   - `http.cookiejar` 吞 Cookie：改为拼原始 `Cookie` 请求头注入，`RAW_COOKIE` 已生效。
7. **租房站锡山/新吴/江阴/宜兴（7-25 上午）**：同一次手动验证窗口连续跑通 4 个整区，各 50 页、0 撞墙，分别灌 D1：锡山 1140 / 新吴 1143 / 江阴 1121 / 宜兴 1154。**至此租房 7/7 全部完成。**

---

## 3. 当前卡点 & 关键认知修正（必读）

**根因：58 对「沙箱出口 IP」在租房频道做了主动验证码墙（code=300）。**

- 58 现在要求浏览器执行 JS 生成新鲜 `fzq_h` 反爬 token。纯 HTTP 请求（urllib）永远过不了。
- 即便用 Playwright 真浏览器，**沙箱的固定出口 IP 被 58 单独列进验证码墙**（页面里能看到 `ws:<沙箱IP>`）。

### ⚠️ 认知修正（重要）
之前结论"用户浏览器验证/复制 Cookie/走代理都救不了沙箱 IP，必须重启光猫"——**部分错了**：
- **证伪（Cookie 粘贴无效，仍成立）**：之前用户给新鲜 Cookie + 代理，沙箱仍返回 `ws:<IP>` 墙。
- **新证据（手动过验证码有效！）**：用户说"手动点了 58 租房验证"后，沙箱爬虫把**滨湖整区 50 页全爬通、0 撞墙**，紧接惠山也整区通。说明**用户在浏览器里交互式过掉验证码，有可能给沙箱出口 IP 开一个宽松窗口**（机制不明，可能是 58 后端按 IP 解封一段时间，或 `fzq_h` 这类 token 跨会话被认可）。
- **代理 `127.0.0.1:10808` 对国内 58 是 no-op**（走直连），改变不了 `ws:` IP，这条仍成立。
- **光猫重启未必换 IP**（CGNAT/共享公网），7-25 上午那次"重启"给的窗口只撑 ~9 页就死；**手动验证才是可靠的刷新方式**。

**续命办法（按可靠性排序）**：
1. **首选：让用户手动在浏览器过一次 58 验证码**，然后立刻直跑目标区。
2. **次选：重启光猫换 fresh IP**（历史有效，但 7-25 上午这次疑似未真换 IP）。
3. 拿到窗口后**宽限额度可能很大且不稳定**：7-24 晚 2 区；7-25 上午一次手动验证竟连跑 **4 个整区全通、0 撞墙**。窗口耗尽后该 IP 再撞墙，需再刷新（仍优先手动验证）。

---

## 4. 标准续跑流程（若日后需刷新数据）

节奏（用户已确认）：**用户刷新窗口（手动过验证码 或 重启光猫）→ 说「重启完毕继续任务」→ 我立刻直跑下一个缺失区。**

**当前进度（已全部完成 ✅）**：
```
✅ 梁溪 liangxi（673）
✅ 滨湖 binhu（1119）
✅ 惠山 huishan（1116）
✅ 锡山 xishan（1140）
✅ 新吴 xinwu（1143）
✅ 江阴 jiangyin（1121）
✅ 宜兴 yixing（1154）
```
> 租房 7/7 全部落 D1（合计 7466 条），看板已可见全城租房数据。无需再补。

**每个区的标准动作（详见 HANDOFF_AGENT.md 的命令）**：
1. 清本地 `listings` 表中本区旧数据（**绝不清全表**，否则会触发爬虫自动重新播种 liangxi 脏数据，见第 5 节坑 #9）。
2. `USE_PW=1 python crawler.py --district <区code> --no-push` 直跑该区。
3. **`DELETE FROM listings WHERE crawl_batch='seed'`**（剔除自动播种的 liangxi 残留）。
4. `python db_to_sql.py wxzf/local-crawler wx 58` 生成 `import_data.sql`（只含本区）。
5. 在 D1 里**只删该区的** `price_records` + `listing_snapshots`（保留其他区），再导入 `import_data.sql`。
6. 用 wrangler 查 count 校验。

**严禁**：拿到新窗口后先做 `--test` / 门禁探针 —— 会把宝贵的窗口额度耗光，导致该 IP 一个区都爬不全。直奔目标区。

---

## 5. 绝对不要再踩的坑（血泪教训）

1. **别再用 `--test` / 探针消耗宽限额度**。每次 fresh 窗口只够 1–2 个租房区（甚至更多），探针一次就废掉一个区的额度。拿到窗口直接 `--district <区>` 跑。
2. **修正：别再死认"浏览器验证救不了沙箱"**。实测用户手动过验证码后沙箱整区 50 页全通。下次先让用户手动过验证码试试，不行再重启光猫。但**单纯粘贴 Cookie / 走代理仍无效**（历史上验证过）。
3. **灌 D1 前必须 DELETE 叶子表**。`price_records` 无唯一约束，`import_data.sql` 用的是**普通 INSERT（非 OR IGNORE）**，所以**重复导入同一区会翻倍**。租房是**分区增量补**，要用「只删该区」的 DELETE（见下）再导入，**不能 `DELETE FROM price_records` 全删**（会冲掉其他区）；也**绝不可把含多区的混合 SQL 直接导入**（见坑 #11）。
4. **每跑一个区前先清本区 `listings`**：爬虫不会自动清表（只有 `CREATE TABLE IF NOT EXISTS` + 按 listing_key 去重插入），不清的话 `import_data.sql` 会混入之前区的脏数据。
5. **Playwright 实例只启一次**。跨区重开浏览器（`pw_close_browser`）即可，绝不在循环里再调 `sync_playwright().start()`，否则第 2 区起报 `asyncio` 错、后续区全跳。
6. **全程 `env -u HTTP_PROXY ...` 直连**。沙箱有死代理（127.0.0.1:10808），wrangler/npm/任何外网命令都要先 unset 代理变量，否则超时/连不上。
7. **用受管 venv 的 python 跑爬虫**（已装 playwright）：`C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe`。受管 runtime 的 `python.exe` 没装 playwright。
8. **`db_to_sql.py` 会丢「无小区名」的房源**：本地 `listings` 1291 条 → SQL 里 1119 条（172 条因社区名为空被过滤）。这是正常行为，不是 bug。
9. **🆕 清表会触发自动播种（seed）污染**：爬虫发现本地 `listings` 为空时，会从最新备份 JSON 自动 `seed_from_backup` 播种 **liangxi 297 条**（batch='seed'）。这些不是本次数据，**推库前务必 `DELETE FROM listings WHERE crawl_batch='seed'` 剔除**，否则会把旧梁溪也推上去。→ 因此**不要 `DELETE FROM listings`（全清）**，用「只删本区」的针对性删除；即便误全清，跑完也记得 strip seed。
10. **Git Bash 里没有 `pgrep`**：判断爬虫是否在跑用 `ps aux | grep crawler.py`，或监测日志文件大小是否停止增长（≥20s 不变即结束）。
11. **🆕 `import_data.sql` 不是 INSERT OR IGNORE，混合导入会翻倍（踩过）**：`db_to_sql.py` 读本地**全部** listings 生成 SQL。若本地含多区（如 binhu+huishan），SQL 就含多区；若只 `DELETE 本区叶子` 再导入，其他区会在 D1 里被重复插一遍（滨湖因此变 2238）。**正确做法：导入前确保本地只含目标区**（跑前 `DELETE FROM listings WHERE district_code != '<区>'`，但别全清见坑#9），或导入前把 SQL 里涉及的所有区叶子都 DELETE。**翻车补救**：按 `listing_key` 去重（见 `dedup_binhu.sql` 模板）。

---

## 6. 关键命令速查

```bash
cd "D:/AI work/workbuddy/2026-07-24-11-46-09/wxzf/local-crawler"
"$PY" -c "import sqlite3;c=sqlite3.connect('local_crawl.db');c.execute(\"DELETE FROM listings WHERE district_code='huishan'\");c.commit()"
env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy USE_PW=1 \
  "$PY" crawler.py --district huishan --no-push > "../wxzf_run_huishan.log" 2>&1
# 跑完 strip seed（清掉自动播种的 liangxi 残留）
"$PY" -c "import sqlite3;c=sqlite3.connect('local_crawl.db');c.execute(\"DELETE FROM listings WHERE crawl_batch='seed'\");c.commit()"
# 生成 import_data.sql（只含本区）
cd "D:/AI work/workbuddy/2026-07-24-11-46-09"
env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy "$PY" db_to_sql.py wxzf/local-crawler wx 58

# 灌 D1：先删本区叶子（替换 ke_code），再导入
# delete_leaves_<code>.sql 内容（注意用 ke_code 不是 code）：
#   DELETE FROM price_records WHERE district_id=(SELECT id FROM districts WHERE ke_code='huishan');
#   DELETE FROM listing_snapshots WHERE district_id=(SELECT id FROM districts WHERE ke_code='huishan');
cd "D:/AI work/workbuddy/2026-07-24-11-46-09/wxzf"
WR="env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2/node.exe C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules/wrangler/bin/wrangler.js"
$WR d1 execute wxzf-db --remote --file=local-crawler/delete_leaves_huishan.sql
$WR d1 execute wxzf-db --remote --file=local-crawler/import_data.sql
# 校验
$WR d1 execute wxzf-db --remote --command="SELECT d.name,count(*) n FROM price_records p JOIN districts d ON d.id=p.district_id GROUP BY d.name ORDER BY n DESC;"
```

区 code 对照（district_id 见 D1 `districts` 表）：`liangxi`梁溪(1) `binhu`滨湖(2) `huishan`惠山(3) `xishan`锡山(4) `xinwu`新吴(5) `jiangyin`江阴(6) `yixing`宜兴(7)

---

## 7. 文件 / 日志索引（卡点时去哪找）

- **项目根**：`D:\AI work\workbuddy\2026-07-24-11-46-09\`
- **租房全量日志**：`wxzf_full_run2.log`（梁溪782后撞墙，exit 0）
- **滨湖本次日志**：`wxzf/wxzf_run_binhu.log`（50页全通、1291条、0撞墙，已推 D1=1119）
- **惠山本次日志**：`wxzf/wxzf_run_huishan.log`（50页全通、1279条、0撞墙，已推 D1=1116）
- **锡山本次日志**：`wxzf/wxzf_run_xishan.log`（7-25 上午，50页全通、1334条、0撞墙，已推 D1=1140）
- **新吴本次日志**：`wxzf/wxzf_run_xinwu.log`（7-25 上午，50页全通、1333条、0撞墙，已推 D1=1143）
- **江阴本次日志**：`wxzf/wxzf_run_jiangyin.log`（7-25 上午，50页全通、1315条、0撞墙，已推 D1=1121）
- **宜兴本次日志**：`wxzf/wxzf_run_yixing.log`（7-25 上午，50页全通、1352条、0撞墙，已推 D1=1154）
- **租房爬虫**：`wxzf/local-crawler/crawler.py`
- **租房本地库**：`wxzf/local-crawler/local_crawl.db`（`listings` 表；历史各区有 `local_crawl.db.bak_<区>_*` 备份）
- **租房导入SQL**：`wxzf/local-crawler/import_data.sql`（最新一次 db_to_sql 生成，仅宜兴 1154；历史各次 SQL 已被覆盖，区数据以 D1 为准）
- **去重脚本**：`wxzf/local-crawler/dedup_binhu.sql`（按 listing_key 去重，滨湖被混合导入翻倍后用它修回 1119；留作模板）
- **删叶子SQL模板**：`wxzf/local-crawler/delete_leaves_<code>.sql`，7 区已齐备（liangxi/binhu/huishan/xishan/xinwu/jiangyin/yixing，均为「只删本区」DELETE）
- **转换脚本**：`db_to_sql.py`（根目录，读 listings → import_data.sql）
- **买房爬虫**：`wx-houseing/local-crawler/crawler.py`；本地库同目录 `local_crawl.db`；`delete_leaves.sql`（全删两叶子表，买房整库重灌用）
- **本会话记忆**：`D:\AI work\workbuddy\2026-07-24-11-46-09\.workbuddy\memory\2026-07-25.md`
- **可复用技能**：`C:\Users\Administrator\.workbuddy\skills\cf-58-scraper-replicate\SKILL.md`（完整复刻/排坑手册）
- **诊断文档**：`ANTIBOT_DIAGNOSIS.md`（反爬网关结论）、`WORKFLOW_REVIEW.md`（架构全景）
- **会话历史**：用 WorkBuddy 的「回忆/搜索历史对话」搜「无锡 58 爬虫 / wxzf / 重启光猫」可定位本会话

---

## 8. 线上资源地址

- 买房看板：https://wx-houseing-worker.wxatp.workers.dev
- 租房看板：https://wxzf-worker.wxatp.workers.dev
- D1 库：`wx-houseing-db` / `wxzf-db`（database_id 见各自 `wrangler.toml`）
- KV：`wx-houseing-worker-cache` / `wxzf-worker-cache`

> 备注：买房站 ✅ 已交付（3685 条）；**租房站 ✅ 已完成 7/7 区（7466 条）**。无锡 58 同城买房+租房全量数据均已落 D1，看板可见全城二手房与租房数据。项目整体交付完毕。

---

## 9. 本文的发布去向 & Handoff 自动发布流程

### 9.1 本次发布去向
- **博客（自动发布）**：人读版已写入 `D:\AI work\daily-note-blog`（`_posts/2026-07-25-handoff.md`，Jekyll + GitHub Pages），已 `git commit`。`git push origin master` 后由 GitHub Actions 自动构建部署到 `https://w662000.github.io/daily-note-blog/`。⚠️ 沙箱连不上 github.com:443，push 由 23:30 自动发布任务在本机联网时完成。
- **语雀（知识库，已发布）**：直连 API 发布到 `w662000/ylv5l7`，标题 `workbuddy-260725-handoff-58-wuxi-58`（已用 API 确认存在于知识库）。
- **Gridea Pro（可发布）**：浓缩版（≤500 字，285 字）已放入 `C:\Users\Administrator\Documents\Gridea Pro\posts\260725-58无锡买房租房项目搭建.md`，`published: true`（⚠️ 坑：Gridea 对 `published:false` 草稿不同步发布，必须 `true`），标题《58的无锡买房租房项目搭建》。
- **handoff 归档副本**：`D:\AI work\workbuddy\handoff\260725_58的无锡买房租房项目搭建_handoff.md`（人读独立归档，命名 `YYMMDD_<Gridea标题>_handoff.md`）。
- **机读版**：同目录 `HANDOFF_AGENT.md`（零上下文可复刻）。

### 9.2 Handoff 自动发布流程（可复用）
- **脚本**：`D:\AI work\workbuddy\handoff\handoff_flow.py`（受管 venv python 运行，幂等可重跑）。流程：自动探测最新 `2026-*` 会话文件夹（或 `--session` 指定）→ 复制 HANDOFF.md 到 handoff 归档目录 → 生成 Gridea 浓缩版(`published:true`) → 写 Jekyll 博客文并 `git push` daily-note-blog → 直连语雀 API 发布到 `w662000/ylv5l7`。换项目时改脚本顶部常量：`GRIDEA_TITLE` / `GRIDEA_SLUG` / `BLOG_TITLE` / `GRIDEA_BODY`。
  - 语雀走**直连 API**（handoff 不频繁，不与每日总结抢 429 额度），不依赖云端 Action、不改 `publish_to_yuque.py`。`requests` 必须 `proxies={"http":None,"https":None}` 绕过沙箱死代理。
  - git push 失败**不致命**（内容落本地仓库，下次 push 即生效）；语雀同名自动跳过。
- **自动化**：已建 `handoff 自动发布（23:30）`（automation id `automation-1784951947030`），每日 23:30 运行 `handoff_flow.py`；当日无 `HANDOFF.md` 则跳过。与既有「每日三端自动发布(23:59:20)」「sync_logs_to_github(23:30)」并行互不干扰（博客 push 已做 `pull --rebase` 重试抗 fast-forward 冲突）。
