---
layout: default
title: 交接文档 · 二、关键决策 _ 注意事项
date: 2026-08-24 23:30:00 +0800
---

# 二、关键决策 / 注意事项

- **日期**：2026-08-24
- **状态**：✅ 已完结（scan 自动收集）
- **来源**：2026-08-24-17-15-37\2026-08-24_每日工作总结.md（日志提炼）

## 项目概述
- **"总结缺失"至少三类成因**：①真关机 ②编排器卡死 ③本机代理断(502)。先查 `automation_runs.error` 字段区分，再决定是否提醒用户修基础设施——不要默认归因为关机。
- **限流场景下"存在性检查失败→全量重发"是错误降级**：只读接口 429 说明写接口更会 429，全量重发只会

## 完成内容
- **"总结缺失"至少三类成因**：①真关机 ②编排器卡死 ③本机代理断(502)。先查 `automation_runs.error` 字段区分，再决定是否提醒用户修基础设施——不要默认归因为关机。
- **限流场景下"存在性检查失败→全量重发"是错误降级**：只读接口 429 说明写接口更会 429，全量重发只会把限流打死并把 run 拖成 4.5 小时。正确做法=列表拉取失败即放弃本轮，等下次调度。语雀脚本此项缺陷已记入待办，本次按约定未改脚本。
- **代理端口不统一**：WB 模型调用走 7897，gh 约定走 10808，两者独立启停。今日 10808 未启动，gh 改用 7897（仅环境变量，未改配置）。建议命令层做"哪个通用用哪个"的自动探测。
- **查询 automation 时 id 前缀要带全**：`automation-1787285013065` 不能漏写前缀，否则误判任务消失。
- **有道云依赖 youdaonote.exe CLI**：未登录时 glm 兜底如实报错不重试；Gridea 浓缩版依赖 glm 凝练，glm 挂则无内容兜底（但 23:15 同夜 glm 兜底仍能推已存在的稿件）。

## 关键产物 / 文件
- **"总结缺失"至少三类成因**：①真关机 ②编排器卡死 ③本机代理断(502)。先查 `automation_runs.error` 字段区分，再决定是否提醒用户修基础设施——不要默认归因为关机。

## 当前状态
（见源文档）

## 后续事项 / 风险
- **限流场景下"存在性检查失败→全量重发"是错误降级**：只读接口 429 说明写接口更会 429，全量重发只会把限流打死并把 run 拖成 4.5 小时。正确做法=列表拉取失败即放弃本轮，等下次调度。语雀脚本此项缺陷已记入待办，本次按约定未改脚本。
