---
layout: default
title: 交接文档 · 测速雷达「多模态榜只有前3名」诊断报告
date: 2026-08-29 23:30:00 +0800
---

# 测速雷达「多模态榜只有前3名」诊断报告

- **日期**：2026-08-29
- **状态**：✅ 已完结（scan 自动收集）
- **来源**：2026-08-29-12-03-48\进度_20260829_1205_测速雷达多模态榜仅3名诊断.md

时间：2026-08-29 12:05
项目：D:\AI work\workbuddy\model-speed-radar（端口 8848）
诊断对象：第 205 轮（2026-08-29 10:12:52）

---

## 一、结论（先给答案）

**不是代码 bug，也不是排名规则被改成 3 名。是本轮 12 个多模态模型里只有 3 个测速成功，TOP5 榜填不满。**

前后端代码写的都是取 5 名，从来没改过：
- `speed_daemon.py:413` — `multi = sorted([...])[:5]`
- `web/app.js:229` — `.slice(0, 5)`（本轮榜）
- `web/app.js:240` — `.slice(0, 5)`（近 N 轮均值榜）

榜上有几条，取决于「本轮有几个多模态模型测成功了」。本轮恰好只有 3 个。

---

## 二、数据核对

| 项目 | 数值 |
|---|---|
| 本轮参与测速模型总数 | 42 |
| 本轮成功 | 19（成功率 45%） |
| 多模态池总数 | 12 |
| **多模态本轮成功** | **3** |

接口 `/api/aggregated` 实测返回：`window: 5/5`，`text: 5` 条，`multi: 5` 条 —— 后端数据是满的。

### 关键区分：页面上有两个多模态榜
- **「本轮」多模态 TOP5**（容器 `podiumMultiCurrent`）→ 现在只有 **3 条**（用户看到的这个）
- **「近 N 轮均值」多模态 TOP5**（容器 `podiumMultiMulti`）→ 现在是满的 **5 条**（把过去几轮成功的数据算进均值了）

---

## 三、12 个多模态模型的去向

### ✅ 健康（3 个）—— 就是榜上那 3 个
| 模型 | 平均 TTFT | 长期失败率 |
|---|---|---|
| glm-4.1v-thinking-flash | 744ms | 0% |
| agnes-2.5-flash | 510ms | 14% |
| @cf/google/gemma-4-26b-a4b-it | 925ms | 14% |

### ❌ 永久失效（6 个，失败率 100%）
| 模型 | 报错 | 性质 |
|---|---|---|
| gemini-3.1-flash-lite | HTTP 400 `User location is not supported for the API use.` | 大陆 IP 地区封锁，无解（除非换出口） |
| gemini-3.6-flash | 同上 | 同上 |
| gemini-flash-latest | 同上 | 同上 |
| thinkingmachines/inkling | HTTP 410 | 模型已下线，应从配置移除 |
| nvidia/nemotron-3-super-120b-a12b | `'latin-1' codec can't encode character '\u2026' in position 13` | **配置 bug，见下节** |
| sensenova-6.8-flash-lite | 慢模型今日冷却 | 常年超时，实测无价值 |

### ⚠️ 时好时坏（3 个）
| 模型 | 长期失败率 | 本轮报错 |
|---|---|---|
| sensenova-6.7-flash-lite | 86% | 连续 4 轮墙钟 >10s，今日冷却 |
| google/gemma-4-31b-it | 57% | 建连超时 10s |
| minimaxai/minimax-m3 | 43% | `Remote end closed connection without response` |

---

## 四、附带的真 bug：apiKey 是残缺占位符

`C:/Users/Administrator/.workbuddy/models.json` 第 27 条：

```json
"id": "nvidia/nemotron-3-super-120b-a12b",
"apiKey": "nvapi-…oR5T"     ← 长度只有 11，中间是 U+2026 省略号
```

这个 key 当初被人为用 `…` 截断保存了。后果：
1. urllib 发请求时 header `Authorization: Bearer nvapi-…oR5T` 含非 ASCII 字符
2. `http.client` 用 latin-1 编码 header，第 13 位（正好是 `…`）直接抛异常
3. 请求根本发不出去 → 每轮必失败，失败率 100%

报错 position 13 完全吻合（`Bearer nvapi-` 是 0~12，`…` 落在 13）。

**已扫描全量：42 个模型中只有这 1 个 key 含非 ASCII 字符。**

这个模型被 `err_kind` 误归为 `network`，掩盖了真实原因（其实是配置问题）。

---

## 五、处置建议（待确认，未擅自修改）

| 优先级 | 动作 | 说明 |
|---|---|---|
| 高 | 补齐 nemotron-3-super 的真实 apiKey，或从 models.json 移除 | 现在每轮白跑一次 |
| 中 | 把 3 个 Gemini 模型移出测速池 | 大陆 IP 永久 400，纯占名额 |
| 中 | 移除 thinkingmachines/inkling（410 下线） | 同上 |
| 低 | 移除 sensenova 两个慢模型 | 常年触发今日冷却，无数据价值 |

### 可选的代码加固（防止同类问题再被掩盖）
在 `speed_daemon.py` 发请求前加一道 key 校验：key 含非 ASCII 就直接判 `err_kind = "config_error"` 并告警，而不是等到 latin-1 崩了再归成 `network`。

---

## 六、附：诊断用过的命令（可复跑）

```bash
# 1. 看当前聚合榜有几条
python -c "import urllib.request,json;d=json.load(urllib.request.urlopen('http://127.0.0.1:8848/api/aggregated',timeout=20));print('window',d['window'],'text',len(d['text']),'multi',len(d['multi']))"

# 2. 看本轮多模态成败
python -c "
import json;d=json.load(open('data/latest.json',encoding='utf-8'))
res=d['results'];alive=[r for r in res if r.get('rank')]
m=[r for r in alive if (r.get('caps') or {}).get('image')]
print('总',len(res),'成功',len(alive),'多模态成功',len(m))"

# 3. 扫全量 key 是否含非 ASCII
python -c "
import json;c=json.load(open('C:/Users/Administrator/.workbuddy/models.json',encoding='utf-8'))
def walk(o):
    if isinstance(o,dict):
        if o.get('id') and o.get('apiKey') and any(ord(ch)>127 for ch in str(o['apiKey'])): print(o['id'])
        for v in o.values(): walk(v)
    elif isinstance(o,list):
        for v in o: walk(v)
walk(c)"
```
