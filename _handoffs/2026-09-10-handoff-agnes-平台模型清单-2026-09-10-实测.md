---
layout: default
title: 交接文档 · Agnes 平台模型清单（2026-09-10 实测）
date: 2026-09-11 23:30:00 +0800
---

# Agnes 平台模型清单（2026-09-10 实测）

- **日期**：2026-09-10
- **状态**：✅ 已完结（scan 自动收集）
- **来源**：2026-09-10-12-50-38\agnes_models_20260910.md

**来源**：`GET https://api.agnes-ai.cn/v1/models`（官方 API，Bearer key）
**可信度**：T0（平台自身接口实时返回）
**数量**：11 个

## 对话模型（6 个）→ `/v1/chat/completions`

| 模型 id | 状态 |
|---|---|
| `agnes-2.0-flash` | 在架 |
| `agnes-2.5-flash` | 在架 |
| `agnes-2.5-pro` | 在架 |
| `agnes-2.5-pro-alpha` | 在架（实验） |
| `agnes-2.5-pro-beta` | 在架（实验） |
| `agnes-3.0-flash` | **新增，已实测可用** |

## 生图模型（2 个）→ `/v1/images/generations`

| 模型 id | 状态 |
|---|---|
| `agnes-image-2.1-flash` | 在架（老版本，已实测） |
| `agnes-image-2.5-flash` | 2.5 系列，未单独实测 |

## 生视频模型（3 个）→ `/v1/videos`（异步）

| 模型 id | 状态 |
|---|---|
| `agnes-video-v2.0` | 在架（已实测） |
| `agnes-video-2.5` | 2.5 系列，未单独实测 |
| `agnes-video-2.5-flash` | 2.5 系列，未单独实测 |

## agnes-3.0-flash 实测结果

- HTTP 200，可用。
- 自报身份：「我是 Agnes-3.0-flash，由 Sapiens AI 开发」→ **厂商是 Sapiens AI**。
- 响应带 `reasoning_content`（思维链），`usage.completion_tokens_details.reasoning_tokens` 单独计推理 token → 是**带推理的 flash 档**。
- 最小调用：
  ```bash
  curl https://api.agnes-ai.cn/v1/chat/completions \
    -H "Authorization: Bearer $AGNES_KEY" -H "Content-Type: application/json" \
    -d '{"model":"agnes-3.0-flash","messages":[{"role":"user","content":"hi"}],"max_tokens":80}'
  ```

## 注意

- 所有模型 `owned_by: custom`、`supported_endpoint_types: ["openai"]` → OpenAI 兼容协议。
- **`created` 字段全是 `1626777600`（2021-07-20 占位值），不能用来判断上线时间。**
- 生成类（image/video）模型走 `/chat/completions` 会返 `404 {"detail":"Not Found"}`，属正常，不是故障。

原始 JSON：`.tmp_agnes/agnes_models_raw.json`
