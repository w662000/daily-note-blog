---
layout: default
title: 技术点 · 进度：两个雷达工作目录固化 + 测速/测限流间隔调整（2026-08-16 17:45）
date: 2026-08-16 23:30:00 +0800
---

# 技术点 · 进度：两个雷达工作目录固化 + 测速/测限流间隔调整（2026-08-16 17:45）

> 来源：260816_进度：两个雷达工作目录固化 + 测速_测限流间隔调整（2026-08-16 17_45）_handoff.md（项目 handoff 1:1 companion，由 techpoint_flow 新方法提炼；读取编码：utf-8）

## 一、技术选型
（源 handoff 未单列技术选型）

## 二、实施要点与关键技术
（源 handoff 未单列实施要点）

### 关键技术要点（自动抽取）
- > 触发词 `强记` 唤起 context-guardian 技能。用户要求：① WB 记住两个雷达工作目录；② 测速间隔改 1 小时、测限流间隔改 2 小时。
- **模型测速雷达**：`D:\AI work\workbuddy\model-speed-radar`（端口 8848，守护 `speed_daemon.py`）
- **模型测限流雷达**：`D:\AI work\workbuddy\model-rate-limit-radar`（端口 8849，守护 `ratelimit_daemon.py`，看门狗 `supervise.py`）
- | 模型测限流雷达 | 86400（每天） | **7200（2h）** | ✅ 已重启生效 |
- 测限流雷达 8849 监听 PID 9288；`last_launch.out`：`扫描间隔 : 7200 秒（每 2.0 小时一轮）`
- PowerShell `Start-Process -ArgumentList` 不会自动给含空格参数加引号，路径 `D:/AI work/...` 被截断成 `D:/AI` 报 SyntaxError → 须在 ArgumentList 内显式嵌入双引号：`` -ArgumentList "`"$SCRIPT`"" ``。

## 三、关键产物与命令
（见源 handoff 关键产物字段）

## 四、如何复现 / 重打
（见源 handoff 重打方法字段）

## 五、后续风险
- 不能直接从 Bash 调 `cmd.exe`（安全策略拦截）。
- PowerShell `Start-Process -ArgumentList` 不会自动给含空格参数加引号，路径 `D:/AI work/...` 被截断成 `D:/AI` 报 SyntaxError → 须在 ArgumentList 内显式嵌入双引号：`` -ArgumentList "`"$SCRIPT`"" ``。
- 最终用 PowerShell（显式引号 + WorkingDirectory + 隐藏窗口 + 重定向日志）成功拉起两个 daemon。
