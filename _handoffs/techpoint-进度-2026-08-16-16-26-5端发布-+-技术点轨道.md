---
layout: default
title: 技术点 · 进度 · 2026-08-16 16:26（5端发布 + 技术点轨道）
date: 2026-08-16 23:30:00 +0800
---

# 技术点 · 进度 · 2026-08-16 16:26（5端发布 + 技术点轨道）

> 来源：260816_进度 · 2026-08-16 16_26（5端发布 + 技术点轨道）_handoff.md（项目 handoff 1:1 companion，由 techpoint_flow 新方法提炼；读取编码：utf-8）

## 一、技术选型
（源 handoff 未单列技术选型）

## 二、实施要点与关键技术
（源 handoff 未单列实施要点）

### 关键技术要点（自动抽取）
- 进程**仍在运行**（日志 `publish_run2_20260816.log` 最后修改 16:26，刚刚）。
- 用 `Mq39iN` 查 TaskOutput 报 not found —— 真实 task_id 记错，但磁盘日志证明进程活着。
- **语雀端撞 429 限流 36 次**：`[Yuque] HTTP 429: Too Many Requests`。语雀服务端限流导致大量篇目语雀端失败。
- 2. **温和补发语雀**：对语雀 429 失败的 handoff 做退避式补发（单篇、间隔、不硬撞限流），补齐后 4 端成功即移 `bak/`。
- 3. 确认第5端有道云执行成功（幂等跳过已有）。
- ⚠️ 语雀 429 限流是真实服务端限流，补发必须温和退避，不可循环硬撞（违反用户「严禁触发限流」红线精神）。
- run2 进程仍在跑，本轮**不启动任何会冲突的新 publish**（含 techpoint）。

## 三、关键产物与命令
（见源 handoff 关键产物字段）

## 四、如何复现 / 重打
（见源 handoff 重打方法字段）

## 五、后续风险
- ⚠️ 语雀 429 限流是真实服务端限流，补发必须温和退避，不可循环硬撞（违反用户「严禁触发限流」红线精神）。
- ⚠️ techpoint publish 的 `git_commit_push` 与 handoff publish 同推 `daily-note-blog` repo，必须**串行**，否则 git push 互相冲突。
- run2 进程仍在跑，本轮**不启动任何会冲突的新 publish**（含 techpoint）。
