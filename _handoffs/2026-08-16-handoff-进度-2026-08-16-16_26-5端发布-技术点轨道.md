---
layout: default
title: 交接文档 · 进度 · 2026-08-16 16_26（5端发布 + 技术点轨道）
date: 2026-08-17 23:30:00 +0800
---

# 进度 · 2026-08-16 16:26（5端发布 + 技术点轨道）

- **日期**：2026-08-16
- **状态**：✅ 已完结（scan 自动收集）
- **来源**：2026-08-16-10-57-55\进度_20260816_1626.md

## 一、当前磁盘快照
| 位置 | 数量 | 说明 |
|---|---|---|
| `handoff/` 顶层剩余 .md | 65 | 待发布/补发（含语雀失败的） |
| `handoff/bak/` 已归档 .md | 69 | 此前 4 端全成功已移入 |
| `techpoint/update/` 待发 .md | 62 | 首批生成的 62 篇技术点，尚未发布 |
| `techpoint/update/bak/` 已备份 .md | 0 | 尚未发布，故无备份 |

## 二、后台发布 run2 状态（venv python 重跑 handoff_flow publish）
- 进程**仍在运行**（日志 `publish_run2_20260816.log` 最后修改 16:26，刚刚）。
- 用 `Mq39iN` 查 TaskOutput 报 not found —— 真实 task_id 记错，但磁盘日志证明进程活着。
- **尚未执行第5端有道云**：日志 `[有道云·第5端]` 命中 0 行（第5端代码在循环外最后才调，run2 还在循环内逐篇处理）。
- **语雀端撞 429 限流 36 次**：`[Yuque] HTTP 429: Too Many Requests`。语雀服务端限流导致大量篇目语雀端失败。
- 后果：语雀失败的 handoff 4 端不全 → 按代码逻辑不进 `bak/`，跑完仍留顶层待补发。

## 三、源码状态（已核实）
- `handoff_flow.py` 第5端代码仍在：第837行 `_run_youdao_5th()` 定义、第915行调用、第917行执行、收尾 `=== publish 完成（含第5端） ===`。
- `techpoint_flow.py` 已修「只复制不 push」假发布 bug（移植 `run()`+`git_commit_push()`），dry-run 验证 62 篇正常。

## 四、下一步（等 run2 自然结束，不冲突启动新进程）
1. run2 跑完后读 `publish_run2_20260816.log` 核验：语雀成功数 / 有道云第5端是否执行 / 移 `bak/` 数。
2. **温和补发语雀**：对语雀 429 失败的 handoff 做退避式补发（单篇、间隔、不硬撞限流），补齐后 4 端成功即移 `bak/`。
3. 确认第5端有道云执行成功（幂等跳过已有）。
4. run2 完全结束后，再跑 `techpoint_flow.py publish`（博客源 git push 须与 handoff 的 git push 串行，避免同一 repo 冲突）+ 第5端 + 移 `update/bak/`。
5. 给用户 5 端最终「成功 / 跳过 / 失败」汇总。

## 五、风险与注意
- ⚠️ 语雀 429 限流是真实服务端限流，补发必须温和退避，不可循环硬撞（违反用户「严禁触发限流」红线精神）。
- ⚠️ techpoint publish 的 `git_commit_push` 与 handoff publish 同推 `daily-note-blog` repo，必须**串行**，否则 git push 互相冲突。
- run2 进程仍在跑，本轮**不启动任何会冲突的新 publish**（含 techpoint）。
