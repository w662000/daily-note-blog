---
layout: default
title: 交接文档 · 进度_20260818_1247 — b.ai 接入 WB（第一阶段完成）
date: 2026-08-18 23:30:00 +0800
---

# 进度_20260818_1247 — b.ai 接入 WB（第一阶段完成）

- **日期**：2026-08-18
- **状态**：✅ 已完结（scan 自动收集）
- **来源**：2026-08-18-12-42-34\进度_20260818_1247.md

## 结论
- b.ai 的 API key 有效，`deepseek-v4-flash` 在 b.ai 侧真实可用（返回 reasoning_content，支持推理）。
- 已把 b.ai 写入 WB 模型配置，**WB 这一端配置完成**，待 WB 重启/自动重载后生效。

## 已落地的改动
- 文件：`C:\Users\Administrator\.workbuddy\models.json`（扁平数组，WB 的模型源配置）
- 新增一条：
  - id: `b.ai/deepseek-v4-flash`（唯一 id，避开已存在的 SenseNova `deepseek-v4-flash`）
  - name: `[b.ai] DeepSeek-V4-Flash`
  - vendor: `b.ai`
  - url: `https://api.b.ai/v1/`（必须带结尾斜杠）
  - apiKey: `sk-jfsms296ktnymp6moszzf841um4rxl8n`
  - supportsReasoning=true / supportsToolCall=true / supportsImages=false
- 备份：`C:\Users\Administrator\.workbuddy\models.json.bak-add-bai-20260818`

## 验证指令（可复测）
```bash
curl -sS -X POST "https://api.b.ai/v1/chat/completions" \
  -H "Authorization: Bearer sk-jfsms296ktnymp6moszzf841um4rxl8n" \
  -H "Content-Type: application/json" \
  -d '{"model":"deepseek-v4-flash","messages":[{"role":"user","content":"ping"}],"stream":false,"max_tokens":30,"temperature":0.7}'
```
→ 返回 200 + choices，确认通。

## 下一步（用户确认 WB 验收后再做）
- 接入 DSH hermes 网关：编辑 `C:\Users\Administrator\.workbuddy\.dsh\settings.yaml`，在 `llm-pi-ai.providers` 下加 `b.ai` provider 块（嵌套格式，用 `apiKeyEnv` 占位真实 key，不写明文）。
