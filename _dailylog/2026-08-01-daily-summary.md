---
layout: default
title: 每日工作总结 · 2026-08-01
date: 2026-08-01 23:30:00 +0800
---

# 每日工作总结 · 2026-08-01

## 一、今日完成事项

### 1. 模型测速雷达 model-speed-radar（今日主线工程）

从零搭了一个常驻的「模型测速雷达」，解决"到底哪个模型最快、哪个根本用不了"这个每天都要问的问题。

- **它是什么**：一个后台守护进程，每小时自动给模型清单里的全部模型发一枪测速，量首包延迟（TTFT），结果实时推到本机网页看板 `http://127.0.0.1:8848`。纯 Python 标准库写的，零第三方依赖。
- **落点**：`D:\AI work\workbuddy\model-speed-radar\`（刻意不放会话目录——它要跨会话常驻）。
- **看板长什么样**：顶部显示上次测速时间、下次倒计时、本轮进度、"立即测一轮"按钮；中间左右双列 TOP5（左＝文本推理类，右＝多模态类），每列还分「本轮」和「近 5 轮均值」两档；下面是完整排行（评级 S/A/B/C、TTFT 条形图、升降箭头、趋势线）；再下面是「能力明细」折叠区和「历次快照时间轴」。前端每 10 秒轮询，有新数据才刷新，不整页闪。
- **温和拨测原则写进代码**（守用户红线）：串行、模型间隔 2.5 秒、每模型每轮只打 1 枪、撞错立刻停不重试、限流冷却 2 轮、无额度冷却 6 轮、手动触发 5 分钟冷却、端口占用即退出防双实例、重启时距上次不足一个周期先等够再测。曲线要平滑靠「近 N 轮均值」，不靠多打几枪。

### 2. 靠雷达查出一批"看着能用、其实不能用"的模型

雷达一开跑就暴露了模型清单里的大量水分，全天围绕这个做了清理和补齐：

- **8 个 GLM「赠送」档其实没额度**：`glm-4.5-air` / `glm-4.7` / `glm-5` / `glm-5-turbo` / `glm-5v-turbo` / `glm-5.1` / `glm-5.2` 等返回 HTTP 429，但业务码是 `1113 余额不足`——在客户端选中它们会直接报错，此前完全不知情。顺带整理出 GLM 错误码归类表：`1113/1112/1261` = 无额度（重试没用，只能充值）、`1305/1302/1303/1304` = 真限流（可重试）。第一版代码把 1113 一刀切当限流，实测后改成解析响应体 `error.code` 分类。
- **StepFun 14 个模型全部搞通**：原来 14 条全失败，根因不是模型下线，而是配置里带了错误的 `stepfun/` 前缀——StepFun 的 `/v1` 端点不认前缀，裸名全可用。写了 `stepfun_probe.py`（list/probe/apply 三阶段）逐个温和探测 52 个候选，23 个跑通，写回 12 个可用条目。
- **补进三家新平台**：根据 OmniRoute 里已接通的平台截图，补齐了活清单缺的三家——Groq（6 个候选实测只有 `llama-3.1-8b-instant` 通）、HuggingFace（从 128 个模型逐个实测确认 6 个可用）、Cloudflare Workers AI（拿到 account_id 后 17 个候选测出 12 个可用）。
- **删掉真下线的、删掉重复的**：NVIDIA NIM 7 个返回 410 Gone 或 404（DeepSeek R1/V3.2、Kimi K2.5、Llama-4-Maverick 等），OpenRouter 2 个无 provider，SenseNova 生图 ID 不存在，OmniRoute 3 个本机代理条目是已有直连版的重复引用——共清理 15 条。
- **全量重新核实文本 / 多模态分类**：按「仅文本输入 = 文本推理类 / 支持图片输入 = 多模态类」逐个核对官方模型卡，纠正了 Gemini 三个（确实是多模态）、Inkling（支持文本+图片+音频）、Gemma 4 全系列（支持图片）、Nemotron Super/Ultra（其实是纯文本）等一批错误标记。

### 3. 一个自己承认判断错了的复盘：Agnes 两个模型是误伤

清理模型时把 Agnes 的 `agnes-image-2.1-flash`（生图）和 `agnes-video-v2.0`（生视频）当"已下线"删了，理由是它们对 `/chat/completions` 返回 404。用户追问后去查平台 `/v1/models` 只读清单，发现**两个模型都还在**，只是不接对话端点、走各自专用端点。

- 教训和 StepFun 那次同构：**chat 端点 404 ≠ 模型下线**，只能证明"这个端点用不了"。删条目前应该先打一次平台 `/models` 只读核对。
- 修复：把 4 个 Agnes 模型全补回（含平台上有但清单里没有的 `agnes-2.0-flash`、`agnes-2.5-pro`）。
- 顺手给雷达加了 `endpoint_mismatch` 归类，把"模型存在但 chat 不接"（琥珀色·接口不符）和"真 410 下线"（灰色·已下线）在看板上分开显示，不再互相污染。

### 4. Agnes 生图 / 生视频从摸索到沉淀成技能

把 Agnes 这两个非对话模型彻底摸清了，中间还推翻过一次自己的错误结论：

- **生图**：`POST /v1/images/generations`，关键坑是 `response_format` 必须塞进 `extra_body` 里（放顶层会 400）；图生图用 `extra_body.image`。免费。
- **生视频**：`POST /v1/videos`（异步），`num_frames` 必须满足 `8n+1` 且 ≤441；创建后轮询 `GET /v1/videos/{task_id}` 看状态。
- **一次被推翻的错误结论**：中途我判定"成片没有直链、必须登录控制台用 cookie 取"，还让用户准备了 cookie。用户丢来官方文档后证明我错了——正确做法是 `GET /agnesapi?video_id=...&model_name=...`，带 API key 就能拿到 mp4 直链。根因是我一直只调状态接口、没调结果接口。这条白折腾提醒：**遇陌生 API 行为第一步应该读官方文档，而不是猜路径**。
- **沉淀为技能** `agnes-media-gen`：以后说「用 agnes 生视频 / 生图」直接加载，不用再推导。技能里把所有坑写死了（extra_body、8n+1、状态接口 vs 结果接口、下载时 Windows SSL 吊销检查要加 `--ssl-no-revoke`、这两个模型对 chat 返 404 属正常）。
- **实跑验证 3 条视频**：韩立庚剑阵（10s / 121 帧 / 72 秒出片）、韩立虚空召唤大庚剑阵（10s / 2 分 11 秒）、韩立 vs 王婵战斗（15s / 185 帧 / 约 4 分钟）。用户实测后还纠正了我一个想当然——**这个模型是会生成简单配音的**（怒吼类音效），我此前基于"一般文生视频不出配音"告诉用户没声音，是错的。

### 5. 黄道十二宫壁纸：从"粗劣涂改画"到 9/10 定稿

这是凌晨到上午的一条主线，前后迭代了 v2→v5 五个版本：

- **v2 失败**：在 AI 已经画了错星座的底图上做遮罩叠图，结果是双层鬼影 + 暗化带成脏斑，用户评价"像粗劣的涂改画"，我自评 3–4/10。**根因**：遮罩擦不干净复杂纹理。
- **v3 换思路**：拆成两步——先让文生图只出**纯净背景**（prompt 明确禁止星座和文字），再在干净背景上从零叠星座。零鬼影，最好的到 8/10。
- **v4 用真数据**：不再手编星座近似形状，改用 IAU 标准星图连线数据；地球辉光从 25% 压到 15%；星座放大近一倍。到 8.5/10。
- **v5 彩虹分宫**：12 宫各给一个独立亮色（色相环均匀分布），解决"星座挨太近看不清"。修了两个 bug——**处女座反子午线环绕**（坐标跨 ±180°，包围盒被算成 360° 宽，整座被压成 10px 扁团，看着像"左右两颗星"）和**相邻星座贴死**（净空只有 1px，拉宽弧线 + 缩小 BOX 后提到 31.7px）。
- **2K 升级**：用户反馈"做壁纸怎么感觉糊了"——底图受文生图接口限制最大只到 1280×800。方案是星座层**矢量重绘到 2560×1440**（坐标/线宽/字号全按比例缩放），底图用 LANCZOS 插值放大。最终自评 9/10。

### 6. 技能开发与多平台上架

- **沉淀技能** `txt2img-vector-overlay`：把上面这套「AI 出氛围背景 + 代码叠精确结构」的方法论封装成可复用技能，核心思想是分工——扩散模型只负责氛围，代码负责结构、文字、计数、清晰度。含通用合成器脚本 + IAU 数据下载脚本 + 三份参考文档（含踩坑清单）。
- **已上架 SkillHub**（skillhub.cn，腾讯技能广场）：评分 4.5/5，安全审核通过。上架踩了两个坑——平台**禁止 `.geojson` 文件类型**（改为运行时从 CDN 下载）、**图标必须正方形**（原 1920×1080 预览图被拒，重做 1024×1024）。
- **已上架联想天禧「苍穹共创计划」**（用户手动走完）。
- **一条重要纠错**：npm 上的 `skillhub` 包是 `@skillhub/cli`，服务的是 Claude/Codex/Cursor 等，**只有 install/search，没有 publish**，跟 WorkBuddy 技能广场毫无关系。之前记忆里误记"它能发布 WB 技能"，已改正。
- **做了 4 个平台的发布包**：ClaudeSkills.ai / SkillHQ / ClawHub / Lenovo，写了生成器脚本按平台调 frontmatter 和定价文案，并有校验脚本把关。关键认知：这四家全部兼容 Anthropic 的开放 SKILL.md 标准，不用重写格式。**但 Coze 不兼容**（它是控制台 UI 表单配置，不是传文件夹），需要另做适配包。
- **Playwright 零下载改造**：用户质疑"本机有 Edge/Chrome，为什么每次要下 150MB Chromium"——用户是对的。改成自动探测系统浏览器优先，找不到才回退。实测全程零下载。
- **顺带产出** 11 个"上传技能有奖励"平台的盘点报告（国内 SkillHub / 联想 / Coze / SkillPie，海外 ClaudeSkills.ai / SkillHQ / Anthropic 官方 / ClawHub / MCPize / Poe / MuleRun）。

### 7. GLM-4.1V 报错三轮排查（一次典型的"以为修好了其实没有"）

用户在客户端切到 GLM-4.1V 后报 `400 max_tokens 参数非法：限制数值范围[1,16384]`，前后查了三轮才真正解决：

- **第一轮**：加了 `maxOutputTokens: 16384`。用户重启后**还是报错**。
- **第二轮（字段名错了）**：重新查实，WorkBuddy 5.2.5 客户端实际读的是 `maxTokens`（驼峰单数），不是 `maxOutputTokens`。改完后 `[1,16384]` 那个错消失了——证明字段名这轮修对了。**自查教训**：上一轮我只读了"接口定义"类文档（官方 API 文档、模型 catalog），没读"踩坑实测"类文档（CSDN 实测文、报错修复贴），所以下错判断。以后查"某字段在客户端里是否生效"，优先信踩坑实测文。
- **第三轮（换了个错，是上下文溢出）**：新报错变成 `inputs tokens + max_new_tokens must be <= 65536. Given: 90196 inputs tokens`。这是完全不同的问题——4.1V 的**总上下文只有 65536**（不是"输入 65536 + 输出 16384"），而当时那个会话已经堆到 90196 tokens。**配置解决不了**，`maxInputTokens` 只是显示字段不是截断开关。唯一解法是开新会话，或换 128K 上下文的 `GLM-4.6V-Flash`（免费视觉档）。
- **21:10 用户确认修复成功**：新会话里 4.1V 4 秒响应、输出干净。

同时把 4.1V 的另一个坑摸清了：它输出重复啰嗦 + 残破 HTML 标签 + 答非所问，**不是配置问题，是模型本身的已知 bug**——THUDM 官方 README 公开承认 overthink / repeat / character escaping。官方在 4.5V 修掉了，而 4.5-Flash 已下线，所以真正的替代是 `GLM-4.6V-Flash`（免费、128K、不强制 CoT、支持工具调用）。

**另一个省钱提醒**：`glm-4.6v-flash`（9B）永久免费不扣额度，而 `glm-4.6v`（106B）是付费会扣资源包的——名字太像容易误切，已把后者改名为 `GLM-4.6V (付费)` 防误点。

### 8. 模型活清单维护

- **换 GLM key**：把 GLM 全部 13 个模型的 apiKey 统一换成新 key，跑了第 9 轮雷达验证。5 个 GLM 用新 key 实测通过；另 8 个显示的是冷却继承的旧状态，单枪直连验证 `glm-5` 返回 200，证明新 key 有额度已生效。没有为了"全面验证"去循环轰 API。
- **清单重排 + 补名头**：74 个模型按国内厂商在前、国外在后重排（StepFun → GLM → Agnes → SenseNova，然后 Gemini / NVIDIA NIM / OpenRouter / Groq / HuggingFace / Cloudflare），并给 28 个缺 `[厂商]` 前缀的条目补上。只改显示名，不动模型 ID。

### 9. 其他

- **agent 指导三方案 + 路由技能**：把项目管理思维工具整理成三套 agent 编排方案（A 多角色并行 / B 情境路由 / C 闭环验证 PDCA），做成可点名的 `agent-plan-router` 技能。以后说「用 A 计划执行」「用 PDCA 执行」就能命中对应方案。今天的 caps 落盘 bug 彻查就是实际用 A 计划跑的（蓝帽定题 + 白帽调查 + 绿帽方案 + 黑帽挑刺）。
- **void.cloud 查实并暂停项目**：用户问注册入口，联网查实 void 是 VoidZero（尤雨溪团队，已并入 Cloudflare）的真实产品，教程内容对得上不是杜撰；但**公开站没有自助注册入口**，是 Early Access 模式。扒完全站源码确认无 waitlist 深链，只有 Discord / GitHub issue 可走。用户决定项目暂停等平台开放。本地全栈示例已搭好、构建通过、无技术阻塞。
- **身份文件收尾**：确认 SOUL.md / IDENTITY.md / USER.md 已落盘本机配置目录，清掉了此前删不干净的 BOOTSTRAP.md。澄清这些只写在本地、未外发。
- **一次日志解析的自我更正**：排查"用户说发过链接但看不到"时，初版脚本只识别 `type=='text'` 漏掉了 `type=='input_text'`，误报"零条含链接"。全量重扫后确认用户确实发过两条链接（7-26 微信公众号文章、7-27 slg 项目页），用户口中的"腾讯文档链接"其实是记忆偏差。
- **评估了用户给的测速脚本**（腾讯文档《流式首包延迟测速脚本》），指出 4 个问题：首包用首字节而非 SSE 首个 content delta 偏低不准、文档说"剔极值"但代码是全量平均、熔断只输出规则文本没真落地、连接级限流可补任务级信号量。这份评估直接催生了今天的测速雷达。
- **00:15 发布 failover 巡检**：目标 7-31，博客与语雀两端均已成功，no-op。

## 二、关键决策 / 注意事项

**决策类**

1. **雷达数据源走"方案 A"**：先把用户手工攒的 14 个 GLM 全并进客户端活清单（新增 10 个、跳过 4 个已存在），然后让雷达直接读活清单。这个决策中途反转过一次——最初判定方案 A 会丢掉 11 个 GLM（含最快的 `glm-4.1v-thinking-flash` 425ms）所以是错的，但用户拍板"先全部导入 WB"之后，前提变了，方案 A 就成立了。
2. **B1/C1 加固暂缓，转观察期**（用户拍板"先不改，观察一下，记录就行"）。病根已经堵死（落盘冻结 caps + 回填历史 + no-store），B1/C1 属加固不属修复，在没有回归测试保护的情况下动三个渲染函数，风险大于收益。已明确列出四个"出现即动手"的触发信号。
3. **删掉 OmniRoute 的 3 个代理条目**：它们指向本机代理 `127.0.0.1:20128`，上游就是清单里已有的直连版，纯重复引用，删了零损失。
4. **规范文档只留 bak 一份**：`Agnes 生图／生视频模型调用规范.md` 顶层和 bak 各有一份会漂移，删掉顶层副本让 bak 成为唯一真源。该文件名不匹配自动发布流程的 `YYMMDD_*` 命名模式，放 bak 是长期静态存放不会被二次移动。

**注意事项 / 今天学到的教训**

1. **chat 端点 404 ≠ 模型下线**（今天栽了两次：StepFun 前缀、Agnes 生图生视频）。删模型条目前必须先打平台 `/models` 只读核对。
2. **平台 `/v1/models` 列出的也不能全信**：StepFun 列了 `step-asr`/`step-tts-mini`/`step-2x-large`，但用 chat 端点打过去全部 404——它们走各自专门端点。**只有实测发 chat 真回文本的才算数。**
3. **遇陌生 API 行为先读官方文档**。Agnes 取片那次，如果早读官方 docs 就不会误判"必须 cookie 登录控制台"，也不用让用户白准备一次 cookie。
4. **查客户端字段是否生效，优先信"踩坑实测"类文档**，不要只看"接口定义"类文档。`maxTokens` vs `maxOutputTokens` 就是这么栽的。
5. **不要凭"一般规律"预设模型能力**。以为文生视频不出配音 → 用户实测有怒吼音效，被打脸。
6. **重归类历史数据靠改 JSON，不重发请求**。今天多次调整错误分类规则后都是写一次性脚本重算已落盘数据，没有为了刷新看板去重打 API——守住了温和测试红线。
7. **Windows 上杀进程的坑**：Git Bash 没有 `pkill`，且 `taskkill` 会遇到路径转换问题，需要 `MSYS_NO_PATHCONV=1 taskkill /PID xxx /F`。
8. **守护进程用 nohup 启动更稳**：用后台任务方式启的进程曾被外部回收导致看板打不开，改用 `nohup ... & disown` 脱离父进程后正常。排查口诀：先 `netstat -ano | grep :8848` 看是否 LISTENING。
9. **改了前端看不到效果 → 先想缓存**。今天有个 bug 表现为"补丁在磁盘上是对的但用户界面还是错的"，真因是浏览器缓存了旧 JS。加 `Cache-Control: no-store` + 资源版本号解决。
10. **技能的 SKILL.md 要精简**。它是运行时加载物，调试过程记录应该放记忆文件而不是塞进技能正文。今天把一个 222 行的技能压到 27 行。

## 三、生成的有用文件

| 文件 / 目录 | 路径 | 用途 |
|---|---|---|
| 模型测速雷达（整个项目） | `D:\AI work\workbuddy\model-speed-radar\` | 常驻守护进程 + 8848 网页看板，每小时自动测全部模型首包延迟 |
| 守护进程主程序 | `D:\AI work\workbuddy\model-speed-radar\speed_daemon.py` | 测速循环 + 内置 HTTP 服务 + 错误归类 + 能力矩阵，纯标准库 |
| 看板前端 | `D:\AI work\workbuddy\model-speed-radar\web\` | index.html / app.js / style.css，原生 JS 无外部库，离线可用 |
| 启停脚本 | `D:\AI work\workbuddy\model-speed-radar\start.bat`、`stop.bat` | 一键启停守护进程 |
| StepFun 探针 | `D:\AI work\workbuddy\model-speed-radar\stepfun_probe.py` | 三阶段 list/probe/apply，把跑通的裸名模型写回活清单 |
| Cloudflare 探针 | `D:\AI work\workbuddy\model-speed-radar\cloudflare_probe.py` | 温和探测 Workers AI 候选模型可用性 |
| 模型能力矩阵 | `D:\AI work\workbuddy\model-speed-radar\MODEL_CAPABILITY_MATRIX.md` | 全量模型按文本推理 / 多模态双列列出输入模态与能力 |
| Bug 彻查报告 | `D:\AI work\workbuddy\model-speed-radar\BUG_A3_20260801.md` | caps 落盘缺失的多 Agent 彻查记录 + 待办 B1/C1 |
| Agnes 调用规范 | `D:\AI work\workbuddy\handoff\bak\Agnes 生图 ／ 生视频 模型调用规范.md` | 唯一真源：端点、参数、坑位、排错表、给模型用的精简提示词 |
| Agnes 视频成片 ×3 | `D:\AI work\workbuddy\2026-08-01-14-48-09\agnes_video_*.mp4` | 韩立庚剑阵 10s / 虚空召唤 10s / 韩立 vs 王婵 15s |
| 黄道十二宫终图 | `D:\AI work\workbuddy\2026-07-31-22-08-47\zodiac_v5_final\color_v3_*.png` | 4 张 2560×1440 彩虹分宫壁纸（翠绿/紫蓝/红紫/极光） |
| 多平台发布包 | `D:\AI work\workbuddy\2026-07-31-22-08-47\publish_packages\` | ClaudeSkills.ai / SkillHQ / ClawHub / Lenovo 四平台适配包 |
| 发布包生成器 | `D:\AI work\workbuddy\2026-07-31-22-08-47\build_publish_packages.py` | 改源技能后重跑即刷新 4 个平台包 |
| 平台奖励盘点 | `D:\AI work\workbuddy\2026-07-31-22-08-47\skill-creator-reward-platforms.md` | 11 个技能上传有奖励平台的对比报告 |
| SkillHub 发布脚本 | `D:\AI work\workbuddy\2026-07-31-22-08-47\publish_skill.py` | Playwright 驱动网页表单，已改为用系统 Edge 零下载 |
| GLM 免费档清单 | `D:\AI work\workbuddy\2026-08-01-11-12-02\GLM免费档清单_2026-08-01.md` | 官方免费档核实清单（区分"免费档"与"赠送额度"） |
| agent 指导三方案 | `D:\AI work\workbuddy\2026-07-31-12-23-31\agent指导三方案.md` | A 多角色并行 / B 情境路由 / C 闭环验证 |
| 技能 · 文生图矢量叠加 | `~\.workbuddy\skills\txt2img-vector-overlay\` | 已上架 SkillHub + 联想，含合成器脚本与踩坑清单 |
| 技能 · Agnes 图/视频 | `~\.workbuddy\skills\agnes-media-gen\` | 说「用 agnes 生视频/生图」直接加载，坑位全写死 |
| 技能 · agent 计划路由 | `~\.workbuddy\skills\agent-plan-router\` | 「用 A/B/C/PDCA 执行」触发对应编排方案 |
| 技能 · 视觉转述 | `~\.workbuddy\skills\glm-41v-visual-transcribe\` | 已压缩至 27 行，含 4.1V overthink 与 65K 溢出规避 |
| 活清单备份 ×8 | `~\.workbuddy\models.json.bak-*-20260801-*` | 今天每次改动前的快照（glm-import / stepfun / add-providers / cleanup / del-omni / add-cf / agnes-add） |

## 四、待办 / 风险

**待办**

1. **Coze 适配包要不要做**——用户尚未回复。Coze 是控制台 UI 表单配置，现有 4 个包不能直接传，需要按它的 schema 另写。
2. **三个平台的技能还没上传**：ClaudeSkills.ai（建议 $9.99，留 90%）、SkillHQ（$9.99，留 85%）、ClawHub（免费曝光）。包已就绪，照各自的 PUBLISH_NOTES 抄表即可。上传前记得把 `author: REPLACE_WITH_YOUR_HANDLE` 换成真实用户名（Lenovo 包已用过无需再动）。
3. **B1 / C1 加固**（观察期中，触发信号出现再做）：B1 = 抽出统一的 `classifyModel()` 单一出口（目前 `renderPodium` 硬依赖 `r.caps.image` 无兜底）；C1 = 造"缺 caps 旧轮次"的回归测试。
4. **8 个 GLM 冷却中的模型**会在后续每小时轮次里自动解除冷却真测，看板会自己变绿，不需要手动干预。
5. **黄道十二宫小瑕疵**：白羊座左边缘略贴边、PIL 直线渲染偏几何风，若要更艺术化可换 SVG 曲线路线。

**风险**

1. **活清单被客户端重新同步的风险**：那 11 个手动并入的 GLM 如果被客户端的模型目录同步清掉，雷达会跟着丢。届时要么重新并入，要么退回"GLM 以用户快照为准"的方案 B。
2. **雷达三处渲染逻辑不一致**（`renderPodium` 无兜底 / `renderTimeline` 三级兜底 / `renderTable` 另一套）——已知技术债，观察期内接受。
3. **`model_gone` 归类仍偏粗**：应进一步细分 `endpoint_mismatch`（模型在但 chat 不接，已做）与 `really_gone`（410 或平台清单查无此模型）。目前 404 的判定还可能冤枉走非 chat 端点的模型。
4. **StepFun 音视频类模型可能被冤枉**：雷达只测 OpenAI 兼容 chat 首包，测不了不等于平台用不了——它们在原生端点上可能正常。
5. **void 项目被动等待**：无技术阻塞，卡在平台账号注册。恢复条件是用户开代理确认 GitHub issue 274 或直接试 `npx void auth login`。
6. **视频生成耗时不稳**：同为 121 帧，一次 72 秒一次 2 分 11 秒，主要卡在排队段；185 帧约 4 分钟。批量生成要留足时间预算。
7. **发布链路 push 失败仍是静默的**（历史遗留项），失败时不会主动告警。

---

*本总结基于当日 7 份机器日志（工作区根目录 1 份 + 会话目录 6 份）提炼整理。*
