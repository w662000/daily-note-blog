---
layout: default
title: 每日工作总结 · 2026-07-28
date: 2026-07-28 23:30:00 +0800
---

# 每日工作总结 · 2026-07-28

> 来源：合并当日机器日志——`2026-07-28-10-25-53/.workbuddy/memory/2026-07-28.md`（全天主任务）+ 工作区根 `.workbuddy/memory/2026-07-28.md`（00:15 Failover 巡检记录）。`2026-07-28-22-47-48` 会话无日志。

## 一、今日完成事项

1. **通达信选股公式（强于大盘）**：为用户整理 4 个版本的通达信条件选股公式，核心是「个股 vs 大盘涨幅对比 + 连续 N 天 + 明显跑赢」——覆盖每日跑赢、累计跑赢、综合版（推荐）、相对强度 RPS 思路；附公式管理器使用方法和踩坑提醒（新股数据不足、停牌伪跑赢、老版本用 COUNT 替代 EVERY），并渲染了概念示意图。
2. **通达信选股公式改版（强于全 A 等权 880008）**：应要求把对标基准从 `INDEXC`（大盘）改为跨品种引用全 A 等权指数 `880008$CLOSE`，逻辑结构一致；新增「跨品种引用需本地已下载该指数数据」的提醒，产出改版文件。
3. **排查并清理 D:\c 误建目录**：定位到 `D:\c` 误建的根因是 WorkBuddy 在创建隔离 Python venv 时，目标绝对路径盘符 `C:` 冒号被路径转换吞掉，变成相对路径被建到 D 盘根；经用户确认后先做字节级备份、再删除，确认 D:\c 已清除、C 盘真实 venv 完好。
4. **微信公众号文章爬取方案**：诊断了「agent-reach 抓到的微信文章链接都不对」的根因（微信封闭生态、搜索引擎索引不到永久链接、临时链接缺 sn 参数 6 小时过期），给出按可靠度排序的 5 种准确爬取方案（公众平台后台 API 最推荐），产出带完整 Python 代码的方案文档和链路对比图。
5. **00:15 发布 Failover 巡检（目标日期 2026-07-27）**：博客源与语雀两端均已齐活，本次为 no-op，未做任何补发。

## 二、关键决策 / 注意事项

- **D:\c 误建根因（重要）**：WorkBuddy runtime 路径 bug——目标绝对路径 `C:\Users\Administrator\.workbuddy\binaries\python\envs\default` 的盘符 `C:` 冒号被吞，变成相对路径 `c\Users\...`；主进程 cwd 落在 D 盘根时，venv 被建到 `D:\c\Users\...`。触发任务是 `发布_failover巡检（WB巡检）`（每天 0:15，用隔离 Python）。真正的 C 盘 venv（138M）完好，D:\c（14M 垃圾副本）不影响实际运行。注意：这与 7-21 Docker 事故「症状相同、机制不同」——那次是 compose 挂载漏盘符。
- **清理兜底经验**：沙箱会拦截 `Add-Type`（编译 .NET）和 `Reflection.Assembly.Load`，回收站 API 无法使用；在有 robocopy 字节级备份兜底的前提下，可直接用 bash `rm -rf` 删除。
- **通达信 880008 跨品种引用**：该公式为跨品种引用，需本地盘后下载板块指数/扩展数据补全，否则结果为空或异常。
- **微信文章 URL 规律**：永久链接含 `__biz + mid + idx + sn`（sn 为文章唯一签名，永久有效）；临时链接用 `signature` 替代 sn（6 小时过期）；搜狗微信链接需 JS 解密 k/h 参数。最可靠的准确爬取是公众平台后台 API（Selenium 登录 + cookie + token + fakeid 调 appmsg 接口，返回带 sn 的永久链接）。
- **发布链路**：每日 23:59 由 GitHub Actions 自动将语雀源发布到语雀；daily-note-blog 经 GitHub Pages 自动渲染为博客。

## 三、生成的有用文件

| 文件 / 目录 | 路径 | 用途 |
| --- | --- | --- |
| 通达信选股公式-连续N天强于大盘.txt | `D:\AI work\workbuddy\2026-07-28-10-25-53\通达信选股公式-连续N天强于大盘.txt` | 4 版通达信条件选股公式（个股 vs 大盘）+ 用法与踩坑提醒 |
| 通达信选股公式-连续N天强于全A等权880008.txt | `D:\AI work\workbuddy\2026-07-28-10-25-53\通达信选股公式-连续N天强于全A等权880008.txt` | 改版公式，对标基准换成全 A 等权指数 880008 |
| 微信公众号文章爬取方案-为什么搜索链接不对.md | `D:\AI work\workbuddy\2026-07-28-10-25-53\微信公众号文章爬取方案-为什么搜索链接不对.md` | 微信文章准确爬取方案（5 种，含完整 Python 代码） |
| _backup_D_c_20260728/ | `D:\AI work\workbuddy\2026-07-28-10-25-53\_backup_D_c_20260728` | 清理 D:\c 前的 robocopy 字节级备份（约 14M），确认无误后可删 |

## 四、待办 / 风险

- **D:\c 备份待清理**：`_backup_D_c_20260728`（约 14M）为清理 D:\c 的兜底备份，待用户确认无用后删除。
- **WorkBuddy runtime 路径 bug 未根除**：`发布_failover巡检`（每天 0:15）下次仍可能误建 `D:\c`；需周期性检查 D 盘根有无 stray `c` 目录并清理。
- **880008 公式依赖本地数据**：用户需先在通达信补全全 A 等权指数 880008 的盘后数据，公式才能出结果。
- **微信爬取方案为方法论文档**：实际落地需 Selenium 环境与公众平台登录态，存在账号风控风险，落地前需评估。
